server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/amber?characterEncoding=utf8
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: amber