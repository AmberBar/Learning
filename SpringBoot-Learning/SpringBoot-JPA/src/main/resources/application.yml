server:
  port: 6666

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/amber
    username: root
    password:
#指定数据库连接池
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
#正向工程
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true